# Copy to .env.local and fill in. .env.local is git-ignored.

# Dashboard login (also used as the session-token HMAC secret)
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=

# Vercel Postgres — pull with:  npx vercel link && npx vercel env pull .env.local
POSTGRES_URL=

# Integrations
GITHUB_TOKEN=
OPENAI_API_KEY=
GOOGLE_SERVICE_ACCOUNT_JSON=
RESPONSE_SHEET_ID=
OUTPUT_SHEET_ID=
